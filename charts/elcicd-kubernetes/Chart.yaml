apiVersion: v2
name: el-cicd-kubernetes
description: |-
  A library chart with the a complete set of Kubernetes resources that might be needed for application deployments.
  Built to be used in conjunction with the elcicd-renderer for rendering as a general Helm-based deployment solution
  across multiple environments.
keywords:
- generic
- parameterized
- profiles

sources: [https://github.com/elcicd/el-CICD-deploy/tree/main/charts]

type: library

version: "0.1.0"