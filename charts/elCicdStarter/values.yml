elCicdChart:
  parameters:
    SOME_PARAM: some-value
    ANOTHER_PARAM: another-value
  
  templates:
  - templateName: DeploymentService
    appName:
    renderProfiles:
      # list of profiles to render template; ignored if empty
    excludeProfiles:
      # list of profiles to exclude template from being rendered
    port:
    env: