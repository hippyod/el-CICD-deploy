apiVersion: v2
name: elcicd-chart
description: |-
  A generic helm chart used to deploy any size group of components onto a kubernetes cluster.

type: application

version: "0.1.0"

dependencies:
- name: elcicd-library
  version: 0.1.0
  repository: file://../elcicd-library